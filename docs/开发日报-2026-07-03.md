# 开发日报 · 2026-07-03

今日共 9 笔提交，涉及 bjt-agent（8 笔，`master-agent-sales`）与 operate-two（1 笔，`operate-agent-sales`）。dev / dev-front / client-two-sqlite 无改动。

## 一、bjt-agent（AI 标书审查平台）

### 1. 登录安全：图形验证码 + 站内注册
- 新增无状态图形验证码服务 `captcha_service`（4 位数字 + 签名 token，自包含校验），`/auth/captcha` 生成、登录前置校验，拦截暴力探测。`76aff7b`
- 前端 `authApi`/auth store 接入验证码；新增 `sendSms` 站内注册短信验证码（桥接运营平台 `/aiGetCode`）。`9196095`

### 2. 草稿文档：选文件即上传解析
- `documents.project_id` 可空 + `owner_user_id` 标记独立文档归属（migration 018）；新增 `/documents` 草稿 router：上传(即时解析)/列表/关联(attach)/删除/内容。`2c9387d`
- 挂载草稿 router，精简前端路由（移除独立 project 详情路由）。`d79ec00`
- 检查页选文件即触发上传解析，点「开始检查」才创建项目并 attach 关联。

### 3. 主题统一：朱红 #D7041A + 移除明暗切换
- 依据 `docs/ui` 设计稿采样主色 `#D7041A`，改根源 CSS 变量 `--blue` 系 + antd `colorPrimary`，清除全站硬编码旧蓝（`#4263eb`/`#4f6ef7`）与旧品牌红（`#d92c4a`/`#c42841`），共 7 文件 43 处。`f0a4726`
- 移除主题明暗切换：删除 `ThemeToggle` 组件与 `useTheme` composable。`fe1fdff`

### 4. 资源与维护
- 入库 UI 设计稿切图（`docs/ui`，41 文件）与前端图片资源（`assets/images/ui`，36 文件，已被登录/检查/套餐等页面引用）。`ef05d70`
- 清理验证码改造后失效的旧登录测试，精简集群维护速查手册。`6d3f430`

## 二、operate-two（运营管理后端）

- **URL 配置外部化**：`RuoYiConfig` 新增 `pluginLoginUrl`/`invokeBaseUrl`，各环境 yml 用环境变量注入；移除 `UserDetailsServiceImpl`/`IHomePageServiceImpl`/`DPromoterInfoServiceImpl` 中硬编码的 `aibjt.com` URL，统一走 `buildInvokeUrl`；交行支付回调 `notifyUrl` 改环境变量。`ec7e7aea`
- **bjt-agent 站内注册桥接**：新增 `SysAiRegisterController`（`aiRegister`/`aiGetCode`），`SecurityConfig` 放行。

## 三、其它（非代码）

- 排查并修复「提交文件解析无结果」：celery worker 与远程 Redis broker 连接假死，`parse_document` 任务积压 parser 队列无人消费；重启 worker 重建连接后全部解析成功。建议落地本地 Redis 根治此复发问题。

## 明日计划

- 本地 Redis 落地，消除远程 broker 不稳定导致的 worker 假死。
- 站内注册流程端到端联调（bjt-agent ↔ operate-two 桥接）。
- 浏览器走查主题色改造后各页面视觉协调性。
