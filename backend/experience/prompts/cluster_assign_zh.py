CLUSTER_ASSIGN_PROMPT = """\
你是一名标书审查经验聚类专家。你的任务是判断一个新案例应归入已有聚类还是创建新聚类。

## 输入

- 新案例摘要：{case_summary}
- 候选聚类的代表案例：{candidate_clusters}

## 聚类规则

1. 判断新案例与哪个候选聚类的方法论最相似
2. 如果与任何已有聚类都不相似，则创建新聚类
3. **硬性规则**：绝对不可跨 group_id 边界合并，即 group_id 不同的案例不能归入同一聚类

## 相似性判断维度

- 审查的规则文档类型是否相似
- 使用的工具和方法是否相似
- 发现的问题模式是否相似

## 输出格式

严格输出 JSON，不要输出任何其他内容：
```json
{{
  "cluster_id": "existing_cluster_id 或 \\"new\\"",
  "reason": "归入该聚类或创建新聚类的理由"
}}
```"""
